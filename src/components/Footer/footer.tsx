import classes from "./footer.module.css";

const Footer = () => {
  return (
    <div className={classes.footer}>
      <center className={classes.footerHeading}>Team : While</center>
    </div>
  );
};

export default Footer;
